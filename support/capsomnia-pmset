#!/bin/zsh
set -euo pipefail

case "${1:-}" in
  on)
    /usr/bin/pmset -a disablesleep 1
    ;;
  off)
    /usr/bin/pmset -a disablesleep 0
    ;;
  display-sleep)
    /usr/bin/pmset displaysleepnow
    ;;
  *)
    printf 'usage: %s on|off|display-sleep\n' "$0" >&2
    exit 64
    ;;
esac
